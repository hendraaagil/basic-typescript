export const pressA = (name: string): string => name + ' pressed => Aa';
export const pressB = (name: string): string => name + ' pressed => Bb';
export const pressC = (name: string): string => name + ' pressed => Cc';